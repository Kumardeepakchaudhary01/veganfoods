function Checkout() {
  return (
    <div>
      <h1>Payment Gateway (Visual Only)</h1>
      <p>Click below to proceed (No real transactions).</p>
      <button>Pay Now</button>
    </div>
  );
}

export default Checkout;
